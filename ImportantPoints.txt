->Shortcut code for plotting the Decision Boundary
from mlxtend.plotting import plot_decision_regions
plot_decision_regions(X_train, y_train.values, clf = model, legend = 2)

Why y_train.values??
Because we are plotting values so it must be in array form